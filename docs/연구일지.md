# 연구일지 (append-only 일기)

> 규칙: **새 엔트리는 맨 아래에 추가한다. 이전 내용은 절대 지우지 않는다.**
> 그때그때의 결정·실측·방향전환을 날짜별로 누적 기록한다.
> 정제된 논문 스토리는 `paper_story.md`, 진행 일기는 이 파일.

---

## 2026-07-08 — 방향 확정: JKICS 투고 + 대역폭 저감 프레이밍

### 큰 결정
- **투고처 = JKICS(한국통신학회논문지, 국문 KCI)**. → 탑 로보틱스 학회 기준의 비관은 해당 없음. 심사자는 통신·IT 연구자.
- **프레이밍 전환: "연산량 감소" → "데이터/대역폭 저감(task-oriented communication)".**
  이유: SUPER는 raycasting-free라 점을 잘라도 CPU가 안 줄어듦(구조적). 반면 **점 66% 저감 = 대역폭/전송량 저감**은 실측 사실이고 통신 저널에 정합.

### 실측으로 확정된 사실 (이번 arc 전체)
- **점 66%↓** (프레임당 64k→22k). 기하학적으로 정확. ← 논문 헤드라인 숫자.
- **CPU**: raycasting-free SUPER는 무변(112%≈116%). raycasting/OGM 켤 때만 ~15%↓(156→138). → "연산량 대폭↓" 주장 금지.
- **속도**: FoV 축소로 안 바뀜(SUPER replan은 점 수 무관, traj-opt+backup 지배). "속도 스토리" 폐기.
- **안전(정적)**: sector는 안전 무손실(SUPER safety-assured가 sensing-cut을 보수화로 흡수). "sector 안전↓→adaptive 복구" 스토리 정적에선 폐기.

### adaptive의 정직한 위치
- **주인공 불가**: CPU 못 줄임, 정적 안전 결손 없음(복구할 것 없음), 동적 blind-zone mover 못 구함.
- **정당한 역할 = task-oriented 저감의 메커니즘**: 항법 태스크에 필요한 방향=진행방향이고 그게 시시각각 변하므로, **고정 sector가 아니라 velocity-정렬 adaptive**가 "태스크 관련 FoV만 유지"를 제대로 달성. 단 이점은 **heading≠velocity**일 때만.

### FoV-awareness 구현 (내가 짠 코드 — 논문 방법론 중심)
- SUPER `FOVChecker`는 OMNI 하드코딩(수직 cut만). 여기에 **수평 ±각 cut 추가**(`fov_horizontal_angle`, `fov_center_heading`; `getFovCheckPlane`에 좌우 평면 2개, `cutPolyByFov` 중심 선택). config `use_fov_cut:true`.
- 효과: 미감지=unknown 처리 → sensing-cut의 대가를 **크래시 → 보수적 정지**로 전환.
- 정량화: dense forest에서 **±60°는 과보수(13m 기어감), ±90°는 완주(86m).** full(360°)은 88m 완주. → 안전-보수화 trade-off 노브.

### 적용 경계 지도 (형이 반례로 직접 규명)
| 시나리오 | 수평 sector 저감 | 이유 |
|----------|------------------|------|
| 정적 전방항법 | ✅ 대역폭 free lunch | 안전·완주 무손실(정적), yaw가 콘을 경로에 맞춤 |
| 동적 장애물 | ❌ 위험 | 꺼진 방향 mover 못 피함, adaptive도 못 구함 |
| 고정기수/벽면검사 | ❌ 커버리지 상실 | 다방향(벽+경로+수직) 필요, 단일 콘으로 못 자름 |
| raycasting-free 플래너 | CPU 무변 | 점 수 무관 |
| occupancy/ESDF 플래너 | CPU↓ | 매핑이 점/광선 비례 |

### 왜 대역폭을 줄여야 하는가 (motivation, JKICS 서론용)
1. LiDAR 데이터량 큼: Mid-360 ~20만점/s ≈ 25–30 Mbps; 고밀도 센서 수백 Mbps.
2. UAV는 무선 전송/오프로딩 多: GCS 전송, 엣지/클라우드 오프로딩(SWaP 제약), 스웜 공유채널.
3. 무선 링크 제한·가변(WiFi/LTE/전용링크, 거리·간섭·스펙트럼 공유) → 초과 시 지연·손실·프레임드롭 → 실시간 제어 위험.
4. 전송 에너지 = 데이터량 비례 → 배터리 제약.
5. → **태스크 인지 저감**(항법에 필요한 FoV만 유지)으로 66%↓, 태스크 무손실. = task-oriented/semantic communication 흐름.
- 정직 단서: LiDAR는 물리적으로 360° 계속 생성 → 필터는 **전송/처리 데이터량**을 줄이는 것(센서 생성량 아님). 동기는 **전송/오프로딩/스웜** 시나리오에 겨눔.

### 논문 구조 (JKICS 국문 초안)
서론(대역폭 동기) / 관련연구(LiDAR 플래너, 데이터 저감, task-oriented comm) / 제안기법(sector 필터+velocity-정렬 adaptive+FoV-aware+SUPER 통합) / 실험(데이터 66%↓·안전·완주·±60/±90 trade-off) / 논의·한계(동적·검사·CPU 경계) / 결론.

### 다음 할 일
- **테스트 = 형이 만든 seed 1~11 맵으로 진행** (형 결정).
- ⚠️ 열린 질문: seed 1~11이 **Gazebo gen_world SDF**(옛 캠페인)인지, **native SUPER용 PCD**인지 확인 필요 — 최근 파이프라인(native SUPER+FoV-awareness+필터)이 어느 쪽에서 도느냐가 갈림.

### [같은 날 추가] 결정: **A = Gazebo `gen_world` SDF (seed 1~11)** 로 통일
- 형이 seed 1~11 맵으로 직접 테스트. 시뮬 = Gazebo/PX4 (기존 캠페인 인프라).
- 해야 할 정렬:
  1. **FoV-awareness는 이미 공유 `super_planner`에 있음**(FOVChecker 수평 cut) → Gazebo config `static_gazebo.yaml`에 `use_fov_cut/fov_horizontal_angle/fov_center_heading` 파라미터만 켜면 됨. (지금은 `static_dense.yaml`에만 넣었음.)
  2. **sector 필터**: Gazebo는 C++ `cloud_preprocessor.cpp`(gz_super_bridge)가 이미 sector+velocity-정렬 구현(2026-07-05). native의 `/tmp/native_sector.py`(python)와 별개 → Gazebo에선 C++ 것 사용.
  3. **측정**: `g_campaign.py`가 이미 seed별·모드별(full/sector/adaptive) points/collision/완주/CPU 측정. 여기에 데이터저감(66%)·±60/±90 trade-off 추가.
- ⚠️ 정직 캐비앗: Gazebo는 **완벽추적이 아니라 SO3 컨트롤러**라 flight-quality 교란(충돌이 컨트롤러 오버슛에 오염될 수 있음). native(perfect_drone)는 깨끗했음. → 논문의 안전 수치는 **모드 간 RELATIVE 비교**로 서술(같은 flight 조건이 full/sector/adaptive에 동일 적용). 이건 기존 캠페인에서도 쓴 논리.
- 다음: Gazebo 파이프라인 생존 점검(seed SDF 존재, static_gazebo.yaml, g_campaign) 후 FoV param 이식 + 실행 명령 정리.

### [같은 날 추가] 파이프라인 점검 완료 + 메인 캠페인(정상 yaw) 결정
- Gazebo 파이프라인 **생존 확인**: seed 1~11 SDF(`/root/px4/.../worlds/default_seed{1..11}.sdf`), `cloud_preprocessor.cpp`(sector±60 + velocity-정렬 adaptive + risk-gate), `g_campaign.py`(모드 full/sector/adaptive), `static_gazebo.yaml` 모두 정상.
- **g_campaign MODES**(코드 확인): `full`=(sector off/360°), `sector`=(고정 ±60° body-forward), `adaptive`=(velocity-정렬 ±60°). 논문 3모드와 일치.
- config 상태: `use_fov_cut:false, max_vel:0.9, yaw_dot_max:1.5, cloud_topic:/cloud_registered` = 검증된 클린 baseline. 섹터필터는 cloud_preprocessor가 `/cloud_registered`로 발행 → SUPER가 구독. **메인 캠페인에 맞음.**
- **결정: (1) 정상 yaw 메인 캠페인 먼저.** 형이 직접 seed 1~11 테스트.
  - 명령: `python3 /root/commands/super_gazebo/g_campaign.py --seeds 1-11 --runs 5`
  - 결과: `results/campaign.csv` — seed·모드별 `pts_mean`(데이터저감), 충돌, 완주, CPU. 분석 `g_analyze.py`.
- **정직 예상**: 데이터 66%↓는 전 seed robust(헤드라인). 충돌은 정상yaw+넓은필드(±15)면 세 모드 다 ~0(안전 무손실 근거). adaptive 이점은 **고정기수(FIXED_YAW)** 조건에서만 → 그건 별도 캠페인(추후).
- 보류 작업: (a) `static_gazebo.yaml`에 FoV-awareness param 이식(부차 실험용), (b) g_campaign에 FIXED_YAW 옵션 연결(adaptive 실험용). 메인 캠페인 후 진행.

### [같은 날 추가] 스모크1 결과 + 발견된 문제 2개 + 수정
**스모크1 (seed9, 1run, 3모드) 결과:**
| 모드 | 완주 | pts | 충돌 | min_clr | fsm CPU |
|------|------|-----|------|---------|---------|
| full | ✅4/4 | 9120 | 1 | 0.21 | 49.8% |
| sector | ✗2/4(타임아웃) | **1828 (80%↓)** | 1 | -0.065(관통) | 111% |
| adaptive | ✗0/4(**이륙실패**) | — | — | — | 118% |

- ✅ **파이프라인 생존, 데이터저감 지표 로깅됨** (full 9120→sector 1828 = 80%↓). 비행 성공 무관하게 잡힘 = 헤드라인 안전.
- ⚠️ **문제1 — CPU 빨간불**: sector가 full보다 CPU 많이 씀(49.8→111%). 원인 = **실패 런 thrashing**(sector 막혀 replan 폭주). native 성공 런에선 sector≈full(112 vs 116)이므로 **실패 런 아티팩트**. 그러나 함의: **SUPER에선 CPU가 sector의 승리 축이 아님(이득 없음, 헤맬 때 악화)** → 논문은 CPU 아닌 **전송 대역폭**으로만 서야 함. CPU는 "정상비행 중립"으로 방어.
- ⚠️ **문제2 — adaptive 이륙실패**: 근본원인 = **g_campaign이 seed당 브링업 1회 + 모드 공유**. sector가 실패해 드론을 망가진 상태(관통/멈춤)로 남김 → adaptive가 그걸 물려받아 climb 실패(z=-0.2, startup timeout). 메모리가 경고한 back-to-back 연쇄 실패.
- 🔧 **수정(g_campaign.py)**: 메인 루프를 **(run,mode)마다 fresh 브링업 + teardown**으로 재구성 + **이륙 실패 시 재시도(`--max-attempts` 기본 3)**. 이제 각 모드가 깨끗한 spawn에서 시작 → 연쇄 실패 제거. 랜덤 이륙 flakiness는 재시도로 흡수. repo scripts/에도 sync.
- 다음: 수정판으로 스모크2 재검증 중(모드마다 fresh 브링업이라 ~20분). 3모드 다 이륙·기록되면 풀 캠페인 GO.
- ⚠️ 트레이드오프 인지: fresh 브링업 per 모드 → 풀 캠페인(11 seed×5 run×3 모드=165 브링업) 수 시간. 신뢰성의 대가.

### [같은 날 추가] 스모크2 결과 — 수정 검증 성공 + CPU 빨간불 해소
**스모크2 (seed9, fresh 브링업 per 모드): 3모드 전부 완주.**
| 모드 | 완주 | pts | fsm CPU | raycast_ms | 충돌 | min_clr |
|------|------|-----|---------|-----------|------|---------|
| full | ✅4/4 (126.7s) | 9039 | 46.3% | 1.83 | 0 | 0.51 |
| sector | ✅4/4 (111.8s) | **3128 (65%↓)** | **39.2%** | 0.63 (**65%↓**) | 0 | 0.39 |
| adaptive | ✅4/4 (101.8s) | **3046 (66%↓)** | **36.7%** | 0.68 | 1 | -0.05 |

- ✅ **CPU 빨간불 해소**: 성공 런에선 sector CPU가 full보다 **낮음**(46→39%). 스모크1의 역전(111%)은 실패 런 replan 폭주 아티팩트 확정. Gazebo 파이프라인은 ROG-Map raycasting ON이라 **raycast 65%↓·매핑 ~50%↓**가 실제로 나옴(예전 G6와 일치). → 논문에서 CPU는 "매핑(raycasting) 비용 감소"로 **겸손하게** 서술 가능, 헤드라인은 여전히 대역폭.
- ✅ **adaptive 이륙 성공**: fresh 브링업 진단 적중(연쇄 실패였음).
- ⚠️ adaptive 1 충돌(min_clr -0.05): 정상 yaw에서 adaptive가 sector 대비 이점 없음(기존 결론 유지). 캠페인에선 상대 비교로 서술.
- ⚠️ **잔여 문제: adaptive 런이 완주 후 자기종료 안 함** → 캠페인이 360s에 강제 kill(데이터는 정상). 원인 = rclpy quirk(타이머 콜백 내 `rclpy.shutdown()`이 spin을 못 푸는 플레이크; sector는 같은 경로로 잘 죽음 → 모드 무관 플레이크). **수정: g_mission exit 경로를 `os._exit(0)`으로** (metrics는 이미 flush됨). 방치 시 풀 캠페인에서 ~4시간 낭비였음.
- 수정 후 adaptive 단독 재검증(스모크3) 진행 중. 통과하면 **풀 캠페인 GO**: `python3 g_campaign.py --seeds 1-11 --runs 5` (예상 소요: 165 (run,mode)×~7분 ≈ **19시간**; --runs 3이면 ~11.5시간).

### [같은 날 추가] 스모크3 통과 — 파이프라인 검증 완전 완료, 풀 캠페인 준비됨
- **adaptive 단독 (exit fix 후): 완주 4/4 (115.2s), 충돌 0, min_clr 0.54, pts 3128(66%↓), raycast 0.62ms. hang 없음 — 사이클 3.7분에 깔끔 종료.**
- 검증된 수정 요약: ① (run,mode)별 fresh 브링업+teardown ② 이륙실패 재시도 `--max-attempts 3` ③ g_mission `os._exit(0)` 종료(rclpy quirk 우회). 모두 repo scripts/ sync됨.
- ⚠️ **CPU% 지표 주의**: 런 간 편차 큼(같은 adaptive 성공 런인데 36.7%↔77.6%). 논문의 연산 지표는 CPU%가 아니라 **프레임당 매핑 시간**(raycast_ms: full 1.83 → sector/adaptive 0.62–0.68 = **65%↓, 런 간 일관**)을 사용. CPU%는 보조(평균±표준편차)로만.
- 소요시간 재추정: hang 제거로 (run,mode)당 ~4–5분 → **--runs 5 ≈ 11–14시간, --runs 3 ≈ 7.5시간.**
- **형이 돌릴 명령**:
  ```
  cd /root/commands/super_gazebo
  nohup python3 g_campaign.py --seeds 1-11 --runs 5 > /tmp/full_campaign.log 2>&1 &
  tail -f /tmp/full_campaign.log        # 진행 확인
  python3 g_analyze.py                  # 완료 후 분석
  ```
  (결과는 results/campaign.csv에 append — 스모크 행들이 섞이지 않게 시작 전 `mv results/campaign.csv results/smoke_$(date +%m%d).csv` 권장)

---

## 2026-07-10 — 🎉 풀 캠페인 완료 (seeds 1~11 × 5 runs × 3 modes = 165런, 12.6시간)

**165/165 완료, 이륙실패 재시도로 기록된 미이륙 행 5개뿐(3%), hang 0건** — 수정된 파이프라인이 밤새 안정 작동.
결과: `results/campaign_full_seeds1-11_x5.csv` + `_analysis.txt` (원본 results/campaign.csv).

### 전체 평균 (모드당 55런)
| 지표 | full | sector | adaptive | sector 감소 | adaptive 감소 |
|------|------|--------|----------|------------|--------------|
| **점/frame** | 8414±1329 | **2974±431** | **2990±338** | **64.6%↓** | **64.5%↓** |
| **Raycast (ms)** | 1.75±0.15 | **0.61±0.06** | **0.63±0.03** | **65.1%↓** | 64.1%↓ |
| ROG Total (ms) | 6.60±1.04 | 2.92±0.41 | 3.16±0.49 | **55.7%↓** | 52.1%↓ |
| fsm CPU (%) | 93.3 | 84.9 | 85.6 | 9.0%↓ | 8.2%↓ |
| **충돌/런** | 1.53 | **0.95** | **0.93** | 38%↓ | 39%↓ |
| min clearance | 0.33 | **0.43** | 0.40 | 개선 | 개선 |
| **성공률(완주)** | 45/55 (82%) | 47/55 (85%) | **52/55 (95%)** | — | **최고** |
| 미션시간(성공런) | 136s | **122s** | 133s | 10%↓ | 2%↓ |

### 핵심 판정 (정직)
1. **데이터 저감 64.6%↓ — 전 seed에서 robust (57~69%).** 논문 헤드라인 확정. n=55, 표준편차 작음.
2. **매핑 비용도 실제로 감소**: raycast 65%↓, ROG 총 매핑시간 56%↓ (Gazebo 파이프라인은 raycasting ON이므로). 프레임당 매핑시간은 런 간 일관(±0.06ms) → 연산 지표로 이걸 사용. fsm CPU%는 9%↓로 겸손하게만.
3. **🔥 예상 뒤집힘 — 안전이 "중립"이 아니라 "개선"**: full이 충돌 더 많음(1.53 vs 0.95/0.93, 38%↓) + clearance도 나쁨(0.33 vs 0.43). 메커니즘 = **full-view가 ROG-Map을 과밀 점유시켜 dense 필드에서 SUPER 계획을 열화**(2026-07-02에 seed7에서 관찰했던 효과가 11 seed × n=55에서 재확인). → 논문에서 "저감해도 안전 유지"가 아니라 **"저감이 오히려 계획 품질·안전을 개선"**으로 쓸 수 있음(단, 이 충돌 수치는 SO3 컨트롤러 트래킹 오차가 섞인 상대 비교임을 명시).
4. **adaptive의 자리 확보 — 완주율 최고 95% (52/55)**: full 82%, sector 85%, adaptive 95%. 충돌도 최저(0.93). 데이터 저감은 sector와 동일(64.5%). → **"velocity-정렬 adaptive = 저감 유지 + 최고 완주율"** — 주인공은 아니어도 정당한 기여로 논문에 들어감.
5. per-seed 노이즈 존재(seed5 sector 충돌 4.0, seed3 adaptive 2.0 등) — 개별 seed 수치는 흔들리나 전체 순서(full 최다 충돌, sector/adaptive 저감+안전)는 유지.

### 논문 숫자 (JKICS 초안용 확정)
- 전송 데이터 **64.6% 저감** (8414→2974 pts/frame, 11맵×55런)
- 매핑 raycast **65.1% 저감**, ROG 총 매핑 55.7% 저감
- 충돌 38%↓·클리어런스 개선·완주율 sector 85%/adaptive 95% vs full 82%
- adaptive: 저감 64.5% 유지하면서 **완주율 최고(95%)**

---

## 2026-07-27 — 🔄 대전환: 메인 플랫폼을 Gazebo → native(MARSIM)로 되돌림

### 계기
사용자 질문 "full보다 왜 adaptive가 완주율이 높은거지? 원본 super는 100퍼 아니야? 충돌도 없고" →
Gazebo 165런에서 full조차 82% 완주·1.53충돌인 게 원 논문(0충돌)과 안 맞는다는 정당한 지적.
내가 "매핑지연→overtime" 가설을 만들었으나 **직접 재검증하니 반대 데이터**(sector가 오히려 overtime↑)가
나와 즉시 폐기 — 검증 없이 그럴듯한 서사를 만들면 안 된다는 교훈.

### 결정적 사실 발견: SUPER 원 논문은 Gazebo를 쓰지 않았다
`SUPER/README.md`: "**MARSIM**이 저희 알고리즘을 가상 시나리오에서 테스트·평가하는 데 핵심 역할을
했다" + "자신만의 시뮬레이터(예: Gazebo)를 쓴다면…" 경고문(=Gazebo는 제3자 이식용 안내, 저자들이
쓴 게 아님). **MARSIM = 우리가 "native"라 불러온 perfect_drone_sim(완벽추적)+marsim_render(사실적
LiDAR 렌더링) 그 자체.** 즉 원 논문의 "1080런 0충돌"은 완벽추적 시뮬레이터 위에서 나온 수치이고,
**Gazebo+자체 SO3 컨트롤러(약한 TWR)는 원 논문에 없는, 우리가 임의로 추가한 레이어**였다.

### 자체구현(Gazebo에 SUPER 참고 알고리즘 직접 구현) 제안 검토 → 기각
사용자가 "자율주행 알고리즘만 SUPER 참고해서 자체구현, Gazebo에서 full/adaptive 비교"를 제안.
장점(자기 컨트롤러에 맞춘 자기일관적 마진) vs 단점(CIRI/MINCO/backup 재구현 필요=수일, 안전보장
이론적 근거 상실, SOTA 통합이라는 강점 상실) 비교 후, **MARSIM이 이미 원 논문과 동일 방법론이라
새 구현 없이 오늘 바로 0충돌이 나온다**는 게 확인되어 자체구현 대신 **플랫폼을 native로 되돌리는
쪽으로 결정.**

### seed 1~11(Gazebo SDF) 지형을 native로 이식
- 사용자 절충안: **seed1~10(형이 설계한 크기·밀도 축)을 PCD로 변환**, **seed11 대신 기존
  dense-forest PCD(원 논문과 동일 지도)를 11번째/대조군으로 사용**.
- `/tmp/csv_to_pcd.py`: gen_world의 `default_seedN.obstacles.csv`(x,y,r 원통)를 읽어 원통 표면을
  점군(원주 방향 ds=0.05, 수직 dz=0.1)으로 샘플링, native가 읽는 ASCII PCD로 저장.
  10개 전부 변환 (`perfect_drone_sim/pcd/seed_maps/seed{1..10}.pcd`, 5.9만~15.5만 점).
- PCD 경로 확인: `ROOT_DIR`(컴파일타임 매크로)이 perfect_drone_sim 소스 디렉토리를 가리켜서
  install/share 심볼릭링크 불필요, 소스 트리에 두면 바로 읽힘.
- **신규 config 세트** (기존 static_dense.yaml은 이번 세션 내내 수정된 상태라 건드리지 않고 별도
  파일로 격리):
  - `static_seedmaps.yaml` (map_size [34,34,6], seed1-10용) / `static_reference.yaml`
    (map_size [15,110,6], 원 회랑 크기, reference용) — 둘 다 use_fov_cut/frontend_in_known_free/
    raycasting.enable **모두 false**(원 논문 기본값), cloud_topic `/cloud_sector`.
  - `perfect_drone_sim/config/seed{1..10}.yaml`: init_position (0,0,1.5), pcd_name
    `seed_maps/seedN.pcd`.
  - `mission_planner/data/loop9.txt`: gen_world의 CORNER_C=9 그대로 재사용한 **4코너 루프**
    (9,9)→(-9,9)→(-9,-9)→(9,-9)→(0,0) — Gazebo와 동일 구조라 회전 시나리오(adaptive 이점 검증
    조건) 보존.
  - `mission_planner/launch/benchmark_seedmap.launch.py`(신규, 파라미터화된 launch — 기존
    benchmark_dense.launch.py는 config_name 인자가 3번 중복 선언되어 있어 사실 CLI 오버라이드가
    안 먹는 죽은 코드였음, 새로 만듦) + `benchmark_reference.launch.py`(reference용, 고정값).
  - `colcon build --symlink-install --packages-select mission_planner super_planner
    perfect_drone_sim` 필요(launch/config는 install에 개별 파일 심볼릭링크라 새 파일은 빌드 필요).

### 완주 판정 인프라 신규 구현
- `/tmp/native_loop_monitor.py`: 웨이포인트 시퀀스(좌표 리스트+switch_dist+timeout)를 받아 odom이
  각 지점에 순서대로 도달하는지 추적, 전부 도달하면 success=True로 JSON 기록. 충돌은 native_coll.py와
  동일 방식(점군-드론 최소거리 < 0.2m)으로 같은 스크립트에서 겸함.
- `/tmp/native_campaign.py`: Gazebo g_campaign.py에서 배운 신뢰성 교훈(모드/런마다 fresh
  재시작+teardown, 이륙실패 시 재시도) 이식한 native용 오케스트레이터. ROG-Map 성능로그
  (`rog_map/log/rm_performance_log.csv`)가 native/Gazebo 공유 코드라 **동일 슬라이싱 로직 재사용**
  (raycast_ms 등 매핑비용 지표 그대로 비교 가능).

### 스모크 검증 → 전체 캠페인(11맵×3모드×5런=165런, 114분) 결과
- **seed1~10 (150런, 모드당 n=50): 완주 50/50, 충돌 0/50 — 3모드 전부 완벽.**
  점 저감 sector 67.8%/adaptive 67.9%, raycast 저감 67.6%/67.6%, 전 10맵 66~70%로 일관.
  **이게 형이 요구한 "원 SUPER처럼 충돌 0·완주 100%"를 정확히 만족하는 메인 결과.**
- **reference(dense forest, 5런/모드): 3모드 전부 이따금 충돌**(full 7/sector 9/adaptive 6, 5런 합).
  **sector 특유의 문제가 아니라 지형(장거리 100m·고밀도) 자체 특성으로 보임.**
  `max_vel` 되돌리기(8.0=진짜 원본값 vs 3.0=이전 세션에서 낮춘 값)를 시도했으나 **8.0이 오히려
  악화**(3/5 성공, 여전히 충돌) → 노브 튜닝 아님, whack-a-mole 재확인 후 중단. max_vel 3.0
  유지(둘 중 더 나은 값). **정직하게 한계로 병기, 억지로 0 만들려 하지 않음** — 메인 결과
  (seed1-10)가 이미 완벽해서 논문 헤드라인엔 영향 없음.
- ⚠️ **static_dense.yaml 원본값 재확인 계기**: `git diff`로 원본(max_vel 8.0) vs 이번 세션 중
  수정된 현재값(3.0) 차이 발견 — 앞으로 native config 만들 때는 항상 `git show HEAD:path`로
  pristine 원본 대조할 것.

### 다음
- **paper_story.md v2 작성 완료** — 메인=native seed1-10(100%/0충돌/67.8%저감), 보조=reference
  (정직한 잔여 변동성), Gazebo 165런은 부록/한계("컨트롤러 정밀도가 지배 변수")로 격하.
- 커밋 예정: PCD 변환기, 신규 config 세트, launch 파일, native_campaign.py/native_loop_monitor.py
  (repo scripts/로 이동), native_campaign_full.csv 결과, paper_story.md v2.

---

## 2026-07-27/28 — 🔍 사용자의 핵심 질문: "SUPER는 어차피 충돌 없는데, sector 쓰는 진짜 이유(연산량)는 검증했나?"

### 문제 제기
사용자: "애초에 super가 충돌횟수도 없잖아. sector를 하는 이유는 받아오는 pointcloud정보량을
줄여서 연산량을 줄이기 위해서인데, 이거를 검증해야하지 않을까?" — 정확한 지적. 지금까지 측정한
건 ①점 개수(67.8%↓)와 ②ROG-Map 매핑단계 시간(raycast/update, 67.6%↓)뿐, **fsm_node 전체
프로세스 CPU(진짜 "연산량")는 native 캠페인에서 한 번도 안 쟀음.**

### CPU 측정 추가 + 재검증 (seed1-10, 3모드×5런=150런, 96분)
`native_campaign.py`에 Gazebo g_campaign.py의 `CpuMeter`(/proc/pid/stat utime+stime 샘플링) 이식 —
fsm_node + 필터 노드(native_sector.py) 둘 다 측정.

**결과 (완주 150/150, 충돌 0/150 — 완전히 클린):**
| | full | sector | adaptive |
|---|------|--------|----------|
| fsm_node CPU% | 99.4 (±2.0) | 97.9 (±1.5) | 97.5 (±2.0) |
| 필터 자체 CPU% | 10.6 | 11.5 | 11.5 |
| **시스템 합계** | **110.0%** | **109.4%** | **109.0%** |

**fsm_node 전체 CPU는 사실상 무변화(1.5~1.9%p, 표준편차 수준 = 노이즈).** 필터 노드 자체가
입력 클라우드 전체를 매번 읽어야 해서 ~10~11% CPU를 상시 소모 → fsm_node에서 아낀 만큼을
필터 오버헤드가 거의 상쇄 → **시스템 전체 CPU는 세 모드가 사실상 동일.**

### 결론 — "연산량 절감" 주장은 실측으로 기각
이번 세션 초반에 이론적으로 예측했던 것("SUPER 리플랜은 궤적최적화가 지배, 점 개수와 무관")이
**이제 직접 측정으로 확정**됨. **"sector가 연산량을 줄인다"는 주장은 이 구현·설정에서 성립하지
않는다.** 유일하게 실측으로 방어 가능한 이득 = **전송 데이터량 67.8% 저감** (연산량 아님).
→ 이건 실패가 아니라 **JKICS 논문의 대역폭(bandwidth) 프레이밍이 처음부터 옳았다는 걸 직접
증명한 것** — 이전엔 추론이었는데 이제 실측 근거.

### 조치
- `paper_story.md`에 §4.2.1(CPU 검증) 신설 + §5·§6을 "연산량 아니라 대역폭"으로 정정.
- `native_campaign.py`(CPU 측정 추가) + `results/native_campaign_seed1-10_cpu.csv` 커밋.

---

## 2026-07-28 — 대역폭 저감의 "실익" 검증: 제약 링크 실시간 전송 가능성

### 문제 제기
사용자: "그럼 전송 데이터량을 줄여서 보는 이득이 뭐가 있는지 해야되는거 아니야?" — 정확한 지적.
"67.8% 줄었다"까지만 보였지, 그게 실제로 뭘 가능하게 하는지는 검증 안 됨.

### 방법: 실측 바이트 + 큐잉 sustainability 계산 (tc 트래픽쉐이핑은 lo 전체에 영향줘서 위험 판단, 대신 안전한 계산 방식 채택)
`ros2 topic bw`로 **실제 메시지 바이트**(추정 아님) 측정:
- full(`/cloud_registered`): **27.15 Mbps** (339KB/frame @ 10Hz)
- sector(`/cloud_sector`): **6.50 Mbps** (81KB/frame @ 10Hz)
- **실측 감소 76.1%** (점 수 기준 67.8%보다 더 큼 — 메시지 오버헤드 구조 차이로 추정, 핵심 결론엔 무관)

센싱주기(10Hz→프레임예산 100ms) 대비 각 링크 대역폭에서 프레임 전송시간을 계산 — 예산 초과 시
지연이 프레임마다 누적(큐 무한성장, 실시간 불가):

| 링크 | full 전송시간 | 유지? | sector 전송시간 | 유지? |
|------|--------------|-------|-----------------|-------|
| 5 Mbps | 543ms | ❌5.4배 | 130ms | ❌1.3배(근접) |
| **10 Mbps** | 271ms | **❌2.7배** | **65ms** | **✅유지** |
| 20 Mbps | 136ms | ❌1.4배 | 32ms | ✅유지 |
| 50 Mbps | 54ms | ✅유지 | 13ms | ✅유지 |

### 결론
**10~20 Mbps대(WiFi 원거리·LTE·UAV 디지털링크의 현실적 범위)에서 full은 10Hz 실시간 전송을
못 버티지만, sector는 버틴다.** 이게 "데이터가 줄면 정확히 뭐가 좋아지는가"에 대한 구체적·측정된
답 — 단순 바이트 감소가 아니라 **제약 링크에서 실시간 스트리밍 자체의 가능/불가능을 가르는 차이**.

### 조치
- `paper_story.md` §4.2.2 신설(대역폭 실익 표), §6 기여요약에 반영, 커밋.

---

## 2026-07-28 (계속) — SUPER 원 논문이 실시간 스트리밍/통신을 다루는지 직접 확인

### 질문
사용자: "기존 super논문에서도 실시간 스트리밍에 대해서 언급한게 있어?" — 우리 대역폭 동기가
SUPER 논문과 정합적인지(혹은 우리가 지어낸 건 아닌지) 확인 필요.

### 검증 방법
`/root/share/scirobotics.ado6187.pdf`(원본 PDF, 이전에 형이 준 것) 발견 → `pdftotext`로 전문
추출(96623자) → bandwidth/stream/wireless/transmit/telemetry/GCS/ground station/operator/
remote monitor 등 전수 검색.

### 결과: 언급 전무
- `bandwidth` 1회 등장하나 **통신과 무관**(바이올린 플롯 커널밀도추정의 통계 용어).
- **"SUPER is designed for high-speed navigation... by relying *solely on its onboard sensing
  and computation*"**(743행) — 온보드 완결형을 명시적으로 전제.
- "transmitted to the control module"(1081행) — **기체 내부** 플래닝→제어 모듈 전달, 무선
  전송 아님.
- GCS/지상국/원격모니터링/스웜공유/텔레메트리 등 전부 0건.

### 결론 — 정직하게 서론(§2) 프레이밍 수정
**SUPER 원 논문은 대역폭·전송을 아예 다루지 않는다.** 우리의 대역폭 동기는 SUPER를
반박/확장하는 게 아니라 **SUPER가 안 다룬 별도 배포 축(점군을 외부로도 보내야 하는 상황:
GCS 감독, 스웜 공유, 엣지 오프로딩)을 추가로 고려**하는 것임을 명시. "SUPER 논문도 이 문제를
지적했다"는 식의 서술은 하지 않음(사실이 아님) — 대신 "SUPER는 온보드 완결형을 전제하지만,
실제 배포에서는..." 으로 정직하게 프레이밍.

### 조치
- `paper_story.md` §2 최상단에 이 검증 결과와 정직한 프레이밍 명시, 커밋.

---

## 2026-07-28 (계속) — "왜 full이 아니라 adaptive인가" 종합 논증 정리

### 질문
사용자: "왜 full로 안하고 adaptive를 해야하는지가 있어야 하는데 그거를 어떻게 하면 좋을거 같아?"
— 지금까지 개별 실측(저감%, CPU, 대역폭실익)은 다 나왔는데 이를 하나의 논증으로 엮은 게 없었음.

### 정리 (새 실험 없이 기존 검증 결과 조립)
| 축 | full | adaptive |
|---|------|----------|
| 완주율/충돌 | 100%/0 | 100%/0 (동일) |
| fsm_node CPU | 99.4% | 97.5% (사실상 동일) |
| 대역폭 | 27.15 Mbps | 6.50 Mbps (76.1%↓) |
| 10~20Mbps 링크 실시간 전송 | 불가 | 가능 |

**논증**: full의 이득(더 넓은 감지)은 안전·완주·연산 어디서도 측정되지 않고, 대역폭 비용만 실재
→ full을 쓸 이유가 없음. sector 대비 adaptive를 최종안으로 채택하는 이유는 별도(이미 확립):
정상조건에서 저감·안전 동일(약우세), 요-제약 조건에서만 추가 이점.

### 조치
- `paper_story.md`에 §4.4(종합 논증) 신설 — §2·4.2·4.2.1·4.2.2를 하나의 표+논증으로 통합, 커밋.

---

## 2026-07-28 (계속) — "왜 실시간 전송이 필요한가" 근본 동기 강화

### 질문
사용자: "그럼 왜 실시간 전송을 해야하는데?" — SUPER 자체는 전송이 불필요하다고 이미 확인했으니
(07-28 앞 항목), 그럼 애초에 왜 전송이 필요하다는 전제로 가는지 근본을 물음.

### 해법: SUPER 논문이 스스로 명시한 응용 사례에서 도출 (지어낸 시나리오 아님)
원문(918~923행) 재확인: "*could progress various autonomous drone applications, including
autonomous exploration, logistics transportation, **infrastructure inspection**, and
**search and rescue operations**...*"

이 중 **수색구조·인프라점검은 임무의 목적 자체가 "사람이 센싱 데이터를 실시간으로 보는 것"**:
- SAR: 착륙 후 데이터 확인하면 골든타임 놓침 — 발견 즉시 구조대에 전달돼야 함.
- 점검: 점검관이 이상 징후를 그 자리에서 봐야 재점검 지시 가능 — 착륙 후 확인은 재방문 비용.

**핵심**: "SUPER는 전송이 불필요하다"(비행 관점)와 "SUPER가 명시한 임무는 전송이 필요하다"
(미션 목적 관점)가 동시에 성립 — 우리 동기는 후자를 메우는 것. 지어낸 GCS/스웜 시나리오
나열보다 SUPER 논문 자체의 응용사례에서 직접 끌어와 훨씬 방어 가능함.

### 조치
- `paper_story.md` §2를 SAR·점검 중심으로 재작성(GCS/스웜/엣지는 부차 근거로 격하), 커밋.

---

## 2026-07-28 (계속) — 예상 반박 선점: "링크를 30Mbps로 넉넉히 잡으면 되지 않나?"

### 질문
사용자: "무선링크를 30mb를 쓰면 해결되는거 아니야?" — full(27.15Mbps)이 30Mbps 링크에선
전송시간 90.4ms(<100ms 예산)로 기술적으로 버틴다는 정확한 지적.

### 답변 (인정 + 4가지 이유로 반박)
**"맞다, 기술적으론 된다"를 먼저 인정.** 단 그 전제("30Mbps급을 항상 확보 가능")가 일반적이지
않은 이유 4가지:
1. **여유 마진 부족**: 90.4/100ms=가동률90% — 실제 링크는 거리·NLOS·간섭으로 흔들려서 짧은
   저하만으로도 예산초과. sector(가동률32%)는 여유 큼.
2. **거리-대역폭 트레이드오프**: 동일 하드웨어도 거리 늘면 SNR↓→대역폭↓(RF 물리법칙) — 근거리
   30Mbps 링크도 SAR·점검 같은 원거리 운용에선 수Mbps대로 떨어짐.
3. **하드웨어 비용·중량·전력**: 30Mbps급 라디오가 6.5Mbps급보다 무겁고 비싸고 전력 소모 큼.
4. **스웜 공유채널**: 동일 30Mbps 채널에서 full은 기체 1대, sector/adaptive는 4대+ 동시 운용.

**정직한 결론**: "sector 없이는 절대 불가능"이 아니라 "sector는 더 넓은 범위의 링크품질·거리·
기체규모·다중기체 상황에서 안정적으로 동작하고, 확보 가능한 링크에서도 훨씬 큰 여유를 준다."
— 과장 안 하고 정확한 강도로 주장.

### 조치
- `paper_story.md` §4.3.1 신설(예상 반박 선점), 커밋.

---

## 2026-07-29 — "reference" → "seed11" 이름 정리, 신규 대형/고밀도 맵 시도 후 폐기

### 배경
사용자: "1~10시드를 11번 시드처럼 넓고 장애물 배치도 많이 해야할거 같은데" → 확인해보니
**진짜 gen_world seed11**(예전 12시드 시절 잔재, 현재 SEED_CONDITION엔 없음)은 1~10과 동일 규모
(±15, 100개, medium+dense급 간격)라 "넓고 빽빽하다"는 기억이 틀렸음. 진짜 크고 빽빽한 건
우리가 "reference"라 부르던 원 논문 dense forest PCD였음(단, 이것도 "장애물 개수"가 아니라
"좁고 긴 회랑 형태 + 자연림 점밀도"가 다른 것 — 개별 나무 클러스터링 추정 결과 90~220개 정도로
seed1-10과 개수 자체는 비슷함, 폭 15m×길이 110m).

### 신규 대형 seed11 생성 시도 → 검증 성공 → 그러나 사용자가 폐기 결정
- gen_world 기반으로 **진짜 크고 빽빽한 새 seed11**을 만듦: `/tmp/gen_seed11_big.py`
  (AREA 46=±23, COUNT 220, radius 0.25, gap 1.1(dense), 루프코너 ±18).
  면적 2.4배·장애물 2.2배(220개), 220/220 배치 성공.
- PCD 변환(`csv_to_pcd.py`) → `seed11.yaml`(drone config) → `static_seed11.yaml`
  (map_size [54,54,6]로 확대, A* map_voxel_num은 planning_horizon=7.0이라 local이라 안 건드림)
  → `loop18.txt`(±18 코너) → `native_campaign.py`에 seed11 특수분기 추가.
- **스모크 검증(watch_native.sh 직접 실행, bash -x 트레이스로 목표시퀀스 확인)**: full 모드로
  (18,18)→(-18,18)→(-18,-18)→(18,-18)→홈 4코너 전부 정상 통과, 완주.
- **정식 오케스트레이터로 3모드×1런 캠페인**: **전부 완주(5/5)·충돌 0/3.** 저감 sector 66.2%↓/
  adaptive 67.9%↓ — seed1-10과 거의 동일한 결과. **"더 크고 빽빽해도 결과가 재현된다"는 강력한
  추가 증거였음.**

### 그러나 사용자 결정: reference→seed11로 이름만 바꾸고 신규 맵은 폐기
사용자 의도가 애초에 "reference(원 논문 지도)를 seed11이라고 부르고 싶다"는 **이름 정리**였음이
확인됨 (처음부터 "1~10 pcd변환 + 11만 기존꺼" 의도와 일치). 신규 대형맵(검증까지 끝났지만)은
그대로 폐기 결정.

### 실행한 조치
- 신규 대형맵 아티팩트 전부 삭제: `default_seed11.sdf/.obstacles.csv`(Gazebo), `seed11.pcd`,
  `seed11.yaml`, `static_seed11.yaml`/`_viz.yaml`, `loop18.txt`.
- `native_campaign.py`: `MAPS`의 `"reference"` → `"seed11"`로 변경, `is_ref` 판정을
  `map_name=="seed11"`로 변경(내부 launch/config 파일명 `benchmark_reference.launch.py`/
  `static_reference.yaml`은 그대로 유지 — 라벨만 바뀜), seed11 특수분기(대형맵용) 제거.
- `watch_native.sh`: 동일하게 `seed11` → reference 경로로 매핑, 대형맵 특수분기 제거.
- **결과 CSV 컬럼값도 변경**: `results/native_campaign_seed1-10_reference.csv`의 map 컬럼
  `reference`→`seed11`로 일괄 치환(파일명 자체는 유지, 히스토리 보존).
- `paper_story.md`: §4.1/§4.3/§5의 "reference" 표기를 "seed11"로 정정.
- **일기(diary)는 append-only 규칙상 과거 항목(reference로 쓰인 부분) 유지, 이 항목만 신규 추가.**

### 교훈
- **"11번 시드"라는 표현이 대화 중 두 가지 다른 대상**(gen_world의 진짜 seed11 vs 원 논문
  reference 지도)을 가리켜서 여러 차례 혼선이 있었음 — 이번에 이름을 통일해서 해소.
- 신규 대형/고밀도 맵 자체는 폐기됐지만, **"더 큰 스케일에서도 100%/충돌0이 재현된다"는 검증은
  실제로 확인했었음** — 필요시 언제든 `/tmp/gen_seed11_big.py` 스크립트로 재생성 가능(단, 다른
  이름/번호로 붙여야 함, 현재 seed11은 원 논문 지도로 확정).

---

## 2026-07-29 (계속) — seed1~10 필드·장애물 크기를 seed11급으로 재설계 + 전체 재검증

### 배경
사용자: "1~10시드도 장애물 크기랑 맵 넓기 11이랑 비슷하게 바꿔봐." — seed11(원 논문 지도)의
면적(15m×110m=1650㎡)과 장애물 크기(추정 나무 반경 ~0.15~0.2m)에 seed1~10을 맞춰서, "필드가
작아서/장애물이 커서 결과가 쉽게 나온 것 아니냐"는 우려를 근본적으로 해소하기로 함.

### 목표 수치 확정 (사용자와 함께 계산)
- **필드 면적**: seed11과 동일 면적의 정사각형 = √1650 ≈ 40.6m → 반폭 **±20m**(사용자가 직접
  암산으로 도출, 검증해보니 정확). 기존 ±15(900㎡) → ±20(1600㎡, seed11의 1650㎡와 거의 일치).
- **장애물 반경**: 크기 축(small/baseline/large)은 유지하되 전체를 seed11급으로 하향 —
  0.15/0.25/0.40 → **0.10/0.15/0.20**.

### 구현 (gen_world.py 원본은 안 건드림 — Gazebo 165런 부록 재현성 보존)
- `/tmp/gen_seeds1_10_v2.py`: gen_world를 import해 AREA=40, SIZES={"small":0.10,"medium":0.15,
  "large":0.20}, CORNER_C=12(루프 코너, 9→12로 필드 비율에 맞춰 확대)를 오버라이드해서 seed1~10
  재생성. 기존 SDF/CSV는 `worlds/backup_v1_seeds1-10/`에 백업 후 덮어씀. **100/100 배치 전부 성공.**
- PCD 재변환(`csv_to_pcd.py`), `loop12.txt`(±12 코너) 신규, `static_seedmaps(.yaml/_viz.yaml)`의
  map_size `[34,34,6]→[46,46,6]`(A* map_voxel_num은 planning_horizon=7.0이라 local이라 안 건드림).
- `native_campaign.py`/`watch_native.sh`: `loop9.txt`→`loop12.txt`, `LOOP_WPS` 좌표 갱신.

### 검증
- **스모크(seed3 baseline, 3모드×1런)**: 3/3 완주·충돌0, 저감 68.4%/69.2% — 새 크기에서도 즉시
  재현.
- **정식 캠페인(10맵×3모드×5런=150런, 121분)**: CPU 측정이 이미 모든 런에 내장돼 있어서
  (`CpuMeter`, 지난 CPU검증 세션에 추가함) **메인+CPU 캠페인이 이 한 번으로 동시에 끝남**
  (예상 ~3시간→실제 ~2시간).

### 최종 결과 (v3, 새 헤드라인)
| | full | sector | adaptive |
|---|------|--------|----------|
| 완주/충돌 | **50/50, 0** | **50/50, 0** | **50/50, 0** |
| pts/frame | 8270 | 2560 (**69.0%↓**) | 2547 (**69.2%↓**) |
| raycast ms | 0.596 | 0.190 (68.1%↓) | 0.189 (68.3%↓) |
| fsm CPU% | 85.6 | 84.2 | 84.5 |

**전 10맵 완전히 클린(50/50, 0충돌) — seed11과 같은 규모로 키워도 결과가 그대로 재현됨.**
저감률도 이전(±15, 반경0.15~0.4)의 67.8~67.9%에서 **69.0~69.2%로 오히려 소폭 상승**(장애물이
작아진 만큼 sector 콘 안에 걸리는 점이 더 줄어든 것으로 추정).

### 대역폭 실측도 재측정 (점 수가 달라져서 필요)
`ros2 topic bw`로 새 지오메트리 기준 재측정: full **18.37 Mbps**(이전 27.15) → sector **3.17
Mbps**(이전 6.50), 감소 **82.7%**(이전 76.1%, 오히려 더 커짐). 실시간(10Hz) 전송 sustain 여부:
**5Mbps부터 sector는 이미 유지**(이전엔 10Mbps부터), full은 20Mbps에서도 겨우 8% 여유로 근소하게
유지 — §4.3.1의 "30Mbps면 되지 않나" 반박도 "20Mbps" 기준으로 갱신(더 타이트해진 마진으로 오히려
논증이 더 선명해짐).

### 조치
- `paper_story.md` v3로 전체 수치 갱신(§4.1/4.2/4.2.1/4.2.2/4.3.1/4.4/6), 헤더 버전 표기 갱신.
- `results/native_campaign_v2_seed1-10.csv`, 갱신된 `native_campaign.py`/`watch_native.sh`,
  `loop12.txt`, `static_seedmaps(.yaml/_viz.yaml)`, `gen_seeds1_10_v2.py` 커밋.
- 구 버전(±15/반경0.15~0.4) 결과는 `results/native_campaign_seed1-10_reference.csv`(구)로 보존,
  git history에도 남아있음 — 완전히 삭제하지 않음.

---

## 2026-07-30 — v3의 밀도 버그 발견·수정 (v4) + 전체 재검증

### 문제 발견 (사용자가 직접 시각 관찰로 포착)
v3에서 필드를 ±15→±20(900㎡→1600㎡)로 키우면서 장애물 개수(COUNT=100)를 그대로 뒀다 —
그 결과 **밀도가 오히려 낮아짐**(0.111→0.0625개/㎡), seed11의 추정 밀도범위(0.055~0.133개/㎡)
아래로 떨어짐. "seed11만큼 촘촘하게" 만드는 게 원래 목표였는데 정반대 결과가 나온 것.
사용자가 `watch_native.sh`로 직접 라이브 시뮬레이션을 보다가 포착: **"1~10까지 11에 비해서
장애물이 부족하고 너무 널널한데, 수정한거 맞아?"** — 수치 계산으로는 안 잡히고 육안 관찰로만
드러난 실수. 즉시 인정하고 수정.

### 수정
- COUNT: 100 → **160** (면적 1600㎡ 기준 밀도 0.10개/㎡, seed11 추정범위의 중간값에 정확히 맞춤).
- `gen_seeds1_10_v2.py`에 COUNT 오버라이드 추가, 나머지(AREA=40, SIZES, CORNER_C=12)는 v3와 동일.
- 10개 시드 전부 재생성(100/100 배치 성공 확인) → PCD 재변환 → 가장 밀도가 높은 조건
  (seed7/8, dense gap=1.1)만 우선 스모크 테스트: **6/6 완주, 충돌 0** — 밀도가 오른 만큼 더
  빡빡해졌는데도 안전 확인됨.

### 정식 캠페인 재실행 (10맵×3모드×5런=150런)
| | full | sector | adaptive |
|---|------|--------|----------|
| 완주/충돌 | **50/50, 0** | **50/50, 0** | **50/50, 0** |
| pts/frame | 12214 | 3880 (**68.2%↓**) | 3833 (**68.6%↓**) |
| raycast ms | 0.863 | 0.278 (67.8%↓) | 0.280 (67.6%↓) |
| ROG 총 매핑 ms | 2.285 | 0.948 (58.5%↓) | 0.975 (57.3%↓) |
| fsm_node CPU% | 83.2 | 81.3 | 81.9 |
| 필터 CPU% | — | 8.1 | 8.0 |
| 시스템 합계 CPU% | 90.6 | 89.4 | 89.9 |
| 평균 미션시간 | 36.8s | 37.0s | 37.0s |

밀도를 세 번째로 다시 올렸는데도(v1→v3→v4) **완주율 100%·충돌 0은 네 번째 지오메트리에서도
그대로 재현** — SUPER의 안전보장이 필드 크기·밀도·장애물 반경 변화에 강건하다는 근거가 계속 쌓임.
CPU 결론(연산량 사실상 무변화, traj-opt 지배)도 동일하게 재확인.

### 대역폭 재측정 (밀도가 올라 점 수가 다시 달라졌으므로 필요)
`ros2 topic bw` 실측: full **29.49 Mbps**(v3: 18.37, 밀도 상승으로 다시 커짐) → sector
**4.68 Mbps**(v3: 3.17), 감소 **84.1%**(v3: 82.7%, 오히려 더 커짐 — 밀도가 오른 게 sector에
유리하게 작용). 실시간(10Hz, 100ms 예산) 유지 여부: full은 **30Mbps에서도 겨우 2% 여유**(98ms)로
근소하게만 버티고, 5~20Mbps 전 구간에서 예산을 1.5~5.9배 초과해 탈락 — sector는 **5Mbps부터
이미 유지**(53%+ 여유). §4.3.1의 "30Mbps면 되지 않나" 반박 논증이 v3보다 더 선명해짐(margin이
더 타이트해짐 + 항공 셀룰러 업링크 실측 문헌 근거를 추가로 보강).

### 조치
- `paper_story.md`를 v3→v4로 전면 갱신(헤더 changelog, §4.1/4.2/4.2.1/4.2.2/4.3/4.3.1/4.4/§5/§6
  전부 새 수치로 교체 — §4.4는 첫 시도에서 old_string이 이미 stale해서 edit 실패, 파일을 다시
  읽고 정확한 현재 내용으로 재시도해 성공).
- 다음: `native_campaign_v4_seed1-10.csv`를 repo에 복사, `gen_seeds1_10_v2.py`(COUNT=160)를
  repo scripts에 반영, 커밋+푸시, 메모리 갱신.

---

## 2026-07-30 (계속) — v5: 필드를 "면적"이 아니라 "미션시간"이 seed11과 비슷하도록 재설계

### 배경
v4까지 seed1~10을 seed11과 면적(1600 vs 1650㎡)·밀도(0.10개/㎡)까지 맞췄는데도, 실제 평균
비행속도를 비교해보니 **seed1(≈2.87 m/s)이 seed11(≈1.40 m/s)의 거의 2배** — 미션시간이
36.9s vs ~71.7s로 크게 벌어져 있었음(max_vel 등 SUPER 설정은 두 지도 동일=3.0으로 확인, 순전히
지형 구조 차이: 짧은 4코너 폐루프 vs 긴 밀집 직선 회랑). 사용자: **"시드1~10도 시드 11처럼 걸리는
시간 비슷하게 맵을 넓혔으면 좋겠어."**

### 설계 전환 (AskUserQuestion으로 사용자와 함께 결정)
면적 매칭 대신 **미션시간 매칭**으로 목표를 바꿈. 같은 평균속도 가정 시 미션시간을 2배로 늘리려면
루프 경로 거리도 2배 필요 — 루프 코너거리 공식 `거리=8.828×C`으로 역산: C=12→약23~24m.
필드-루프 여백(기존 8m, v3/v4와 동일 비율)을 유지하는 방식(추천안)을 사용자가 선택 → **필드
반폭 20→32m(전체 64m, 4096㎡ — seed11의 1650㎡보다 오히려 2.5배 커짐)**. 밀도 0.10개/㎡ 유지
위해 장애물 개수 160→**410개**. "면적이 같다"는 이전 논거는 폐기하고 **"미션시간이 비슷하다"**는
새 논거로 대체.

### 구현
- `gen_seeds1_10_v2.py`: AREA 40→**64**, COUNT 160→**410**, CORNER_C_NEW 12→**24**로 갱신
  (SIZES/GAPS는 v4와 동일 유지). 10/10 배치 전부 성공.
- PCD 재변환(410개 장애물 × 10시드 → seed1은 165230점까지).
- `mission_planner/data/loop24.txt` 신규(±24 코너), `native_campaign.py`/`watch_native.sh`의
  `loop12.txt`→`loop24.txt`, `LOOP_WPS`를 `"24,24;-24,24;-24,-24;24,-24;0,0"`으로 갱신,
  `LOOP_TIMEOUT` 150→**300s**(미션시간이 늘어난 만큼 타임아웃 마진도 비례 확대).
- `static_seedmaps.yaml`/`_viz.yaml`의 `map_size` `[46,46,6]`→**`[70,70,6]`**(필드 ±32+버퍼3m),
  super_ws 원본과 super_patches 리포 미러 양쪽 다 갱신. `mission_planner` 패키지만
  `colcon build --symlink-install`로 재빌드(신규 파일 loop24.txt를 install 심볼릭 링크에
  반영하기 위함 — yaml은 기존 심볼릭 링크라 재빌드 불필요, 과거 메모: 신규 파일만 재빌드 필요).

### 검증
- **스모크(seed7/8 최고밀도, 3모드×1런=6런)**: 전부 완주, 충돌 0 (min_clearance 0.36~0.43m).
  **미션시간 75.1~78.2s** — 목표(seed11 ~70~72s)에 근접, 예측치(~74s)와도 일치. 저감률 sector
  67~68%↓/adaptive 68~69%↓로 v4와 비슷한 수준 유지.
- **정식 캠페인(10맵×3모드×5런=150런, 220분)**: full 모드 1건이 시작 단계 타임아웃(300s 내
  waypoints_reached=0/5, min_clearance=3.4m로 애초에 장애물 근처도 못 감 — 충돌 아닌 순수
  인프라 플래키니스, 과거 Gazebo 165런에서도 같은 종류의 5건 관측된 패턴과 동일 성격).

### 최종 결과 (v5)
| | full | sector | adaptive |
|---|------|--------|----------|
| 완주/충돌 | **49/50(인프라1건), 0** | **50/50, 0** | **50/50, 0** |
| pts/frame | 12740 | 4106 (**67.8%↓**) | 4054 (**68.2%↓**) |
| raycast ms | 0.900 | 0.294 (67.3%↓) | 0.295 (67.2%↓) |
| ROG 총 매핑 ms | 2.552 | 1.021 (60.0%↓) | 1.042 (59.2%↓) |
| fsm_node CPU% | 88.8±1.4 | 87.0±2.0 | 87.1±1.3 |
| 필터 CPU% | 7.2±0.2 | 7.7±0.3 | 7.7±0.2 |
| 시스템 합계 CPU% | 96.0±1.4 | 94.7±2.0 | 94.8±1.3 |
| 평균 미션시간 | **76.6s** | **76.3s** | **76.3s** |

**목표 달성: seed1~10 미션시간이 76.3~76.6s로 seed11(70~72s)에 근접.** 다섯 번째 지오메트리
변경(v1→v3→v4→v5)에서도 완주율·충돌 결론은 그대로 재현 — SUPER 안전보장의 강건성 근거가 더
쌓임. CPU 결론(변화가 표준편차 이내, 유의하지 않음)도 동일하게 재확인.

### 대역폭 재측정 (방법 개선: ros2 topic bw의 반올림된 MB 표시 대신, rclpy로 메시지 payload
바이트를 직접 측정하는 스크립트로 전환 — 정밀도 향상)
3회 측정 평균: full **32.62 Mbps**(v4: 29.49, 필드 확대로 다시 소폭 증가) → sector **5.80
Mbps**(v4: 4.68), 감소 **82.2%**(v4: 84.1%, 소폭 하락하지만 여전히 매우 큼). 실시간(10Hz,
100ms 예산) 유지 여부가 v4보다 오히려 더 뚜렷해짐: **full은 이제 30Mbps로도 부족**(109ms>
100ms, v4에선 30Mbps가 겨우 버텼음)하고 **최소 40Mbps 이상이 필요**, sector는 **10Mbps부터
유지**(42% 여유, v4의 "5Mbps부터 유지"보다는 다소 보수적이지만 여전히 full 대비 훨씬 낮은
요구치). §4.3.1의 "링크를 넉넉히 잡으면" 반박 기준이 30→40Mbps로 상향.

### 조치
- `paper_story.md`를 v4→v5로 전면 갱신(헤더 changelog, §4.1/4.2/4.2.1/4.2.2/4.3/4.3.1/4.4/§5/§6
  전부 새 수치·새 프레임(면적매칭→시간매칭)으로 교체).
- 다음: `native_campaign_v5_seed1-10.csv`를 repo에 복사, `gen_seeds1_10_v2.py`(v5, COUNT=410/
  AREA=64/CORNER=24)를 repo scripts에 반영, `loop24.txt`를 super_patches 미러에 반영, 커밋+푸시,
  메모리 갱신.

---

## 2026-08-03 — v6: seed1~10 간격 1.00 m 통일 + 장애물 크기 5단계화

### 결정 배경

seed마다 간격과 반경을 동시에 바꾸면 어떤 요인이 결과를 만든 것인지 분리하기 어렵다는 문제를
정리했다. 사용자 결정에 따라 모든 지도에서 장애물 **표면 간 최소 간격을 1.00 m로 통일**하고,
미세한 0.10 m 변화 대신 장애물 크기 차이를 충분히 크게 둔 단일 크기 축으로 전환했다.

### v6 조건

- 공통: 64 x 64 m 필드, 원통 410개/맵, 루프 코너 +/-24 m, 표면 간격 >=1.00 m.
- seed1--2: 반경 0.150 m, 지름 0.30 m.
- seed3--4: 반경 0.275 m, 지름 0.55 m.
- seed5--6: 반경 0.400 m, 지름 0.80 m.
- seed7--8: 반경 0.525 m, 지름 1.05 m.
- seed9--10: 반경 0.650 m, 지름 1.30 m.
- 인접 단계 차이: 반경 0.125 m, 지름 0.25 m(모델 드론 지름 0.40 m의 62.5%). 각 단계에
  독립 deterministic seed 2개를 둔다.

여기서 1.00 m는 장애물 표면끼리의 거리이며 안전거리나 충돌판정값이 아니다. 동일 반경 `r`인
원통 중심끼리의 명목상 최소 거리는 `1.00 + 2r`이다. 생성 시에는 좌표를 1 mm 단위로 저장해도
1.00 m가 보존되도록 0.002 m guard를 더한다. 반경 증가 때문에 보호구역이 줄지 않도록 장애물 표면
기준으로 이륙점 3.0 m, 각 루프 코너 2.5 m를 비운다.

### 생성·보존

- 생성 명령: `python3 scripts/native_campaign/gen_seeds1_10_v2.py`.
- 추적 가능한 원본 좌표: `scripts/native_campaign/seed{1..10}_static.csv`.
- 같은 좌표로 Gazebo SDF/sidecar와 native MARSIM PCD를 함께 갱신한다.
- 교체 전 v5 world/sidecar는
  `/root/px4/PX4-Autopilot/Tools/simulation/gz/worlds/backup_v5_before_uniform_gap_size_sweep`,
  v5 PCD는
  `/root/super_ws/src/SUPER/mars_uav_sim/perfect_drone_sim/pcd/seed_maps/backup_v5_before_uniform_gap_size_sweep`
  에 최초 1회 백업한다.

### 결과 해석 경계

기존 `native_campaign_v5_seed1-10.csv`와 `paper_story.md`의 수치는 v5 지형 결과로 그대로 보존한다.
v6는 장애물 조건 자체가 달라졌으므로 v5 행을 v6로 이름만 바꾸거나 두 버전을 합쳐 집계하지 않는다.
endpoint smoke는 구동 확인용일 뿐이며, v6 헤드라인 결과는 seed1~10 전체와 비교 대상 모드 전체를
새로 실행한 캠페인으로 산출해야 한다. 상세 사양은 `docs/native_seed1_10_v6.md`에 고정했다.

### 상한 사전 보정

첫 후보는 반경 0.20/0.35/0.50/0.65/0.80 m(반경 차이 0.15 m)였다. 그러나 최대 단계 seed9의
endpoint smoke에서 full도 완주 중 2회 접촉하고 중심--표면 최소거리가 0.016 m까지 감소했다.
FSM RSS도 약 4.2~4.4 GB였다. 다만 최종 r=0.65에서도 planner RSS는 비슷했으므로 메모리 절감을
제외 근거로 삼지 않고, full baseline 충돌로 필터 효과를 분리할 수 없다는 점을 기준으로 r=0.80 m를
메인 sweep에서 제외해 위의 0.150~0.650 m 대칭 5단계로 보정했다.
seed10에서도 full 4회 접촉이 재현됐다. 이 후보 결과는
`results/native_seed1_10_v6_r080_candidate_stress.csv`에 메인 성능값이 아닌 별도 geometric stress
진단으로만 보존한다.

최종 자산 검증에서 seed별 장애물 410개, 저장 파일 기준 최소 표면 간격 1.001728~1.009272 m,
이륙점 표면 여유 최소 3.001362 m, 코너 표면 여유 최소 2.518039 m를 확인했다. seed1--10 PCD는
단계별 241,490/444,850/635,500/838,860/1,042,220점(각 2맵), 합계 6,405,840점이다.

최종 상한 r=0.650 m의 endpoint 1회 smoke(`results/native_seed1_10_v6_final_endpoint_smoke.csv`)에서
seed9/10 full은 모두 완주·충돌 0(중심--표면 최소거리 0.224/0.237 m), adaptive는 모두 완주했지만
각각 1/2회 접촉(0.169/0.006 m)을 기록했다. 따라서 full baseline까지 무너진 r=0.80보다 메인 상한으로
적절하면서도 필터 차이를 드러내는 난이도다. 단, 셀당 1회인 구동 확인이므로 충돌률 결론은 전체
seed1~10 반복 캠페인을 새로 실행한 뒤에만 낸다.

---

## 2026-08-04 — v6 seed1~11 5회 반복 캠페인(165런)

### 무엇을 시도했나

8월 3일의 모드당 1회 결과가 실행 위상에 민감했기 때문에, 현재 v6 지도를 바꾸지 않고
seed1~11×full/sector/adaptive×5회, 총 165런을 새 CSV로 실행했다. seed1~10은 표면 간격
1.00 m를 고정하고 반경 0.150~0.650 m를 변화시킨 원통 410개 맵이며, seed11은 원 논문
dense-forest PCD이다.

### 실측 결과

seed1~10 150런에서 full 49/50, sector 50/50, adaptive 49/50가 완주했다. 최신 LiDAR 표면점이
드론 중심 0.20 m 안에 한 번이라도 들어온 “접촉 표시 런”은 full 15/50(30%), sector
8/50(16%), adaptive 7/50(14%)이었다. seed1~3은 전 모드 0건이었고, 반경이 큰 seed7~10에
접촉이 집중했다. seed11은 full 4/5 완주·5/5 접촉 런, sector 5/5·2/5, adaptive 5/5·1/5였다.

seed1~10의 평균 점수는 full 23,820, sector 7,217, adaptive 7,034로 sector 69.7%, adaptive 70.5%
저감이었다. raycast시간은 1.430→0.478/0.473 ms, ROG 총 mapping은
5.218→1.935/1.975 ms로 줄었다. 완주 런 미션시간은 79.05/78.74/78.82 s로 동일한
수준이었다. fsm CPU는 86.5/85.0/85.3%, 필터 CPU는 7.9/8.3/8.6%로 CPU 절감은 주장할
수 없다.

### 왜 이렇게 나왔나

반경이 커지면 같은 1 m 표면 간격이어도 명목 미션선을 막는 장애물과 회피 분기가 늘어난다.
선행 100 Hz odom 정밀분석에서 접촉은 약 3 m/s에서 발생했고, 몸체 여유 1 m에서 접촉까지
0.34~0.38 s밖에 없었다. adaptive 충돌 대상도 속도 콘 안에 있어 필터 누락으로만 설명되지
않았다. 미관측 공간을 free로 계획하는 설정, 가림, 실효 맵 갱신률, 0.1 s 선행 궤적 커밋이
겹친 것이 가장 타당한 공통 메커니즘이다.

원시 `collisions` 합계는 물리 충돌 횟수로 쓰지 않았다. 현 모니터는 원통 안쪽에서 최신 표면점
거리가 다시 0.20 m보다 커지면 하나의 관통을 여러 번 셀 수 있다. 따라서 이번 표에서는 중복에
강건한 “접촉이 한 번이라도 표시된 런 수”를 사용했다.

### 결론과 다음 작업

adaptive는 데이터 저감이 가장 크고 접촉 런 비율이 가장 낮았지만, sector와의 14% 대 16%
차이는 작다. v5의 “0충돌·안전성 무손실” 결론은 v6에 적용하지 않고 폐기했다. 다음에는
실제 고체 원통 signed distance로 충돌을 판정하고, cloud/occupancy/A* path/position command를 함께
기록해 “못 봤는지”와 “봤지만 못 피했는지”를 분리해야 한다.

---

## 2026-08-04 — full이 sector/adaptive보다 접촉 런이 많은 이유 재분석

### 질문과 분석 방법

5회 캠페인의 seed1~10에서 full 15/50, sector 8/50, adaptive 7/50 접촉 런이 나왔다.
“360°를 모두 보는 full이 왜 더 나쁘나”를 확인하기 위해 각 런의 ROG performance 로그 행 증가량을
미션시간으로 나누어 실효 맵 갱신률을 계산했고, seed·모드별 점수와 대조했다.

### 결과

- seed1~10 평균: full **3.88 Hz**, sector **4.87 Hz**, adaptive **4.89 Hz**.
- seed10: full **1.56 Hz**, sector **2.64 Hz**, adaptive **3.09 Hz**.
- seed10 평균 점/frame: full 35,913, sector 10,888, adaptive 10,537.
- 3 m/s 이동에서 seed10 한 갱신 사이 이동거리: full 약 **1.92 m**, sector 1.14 m,
  adaptive 0.97 m.
- seed4~6의 접촉 런은 full 4건, sector/adaptive 0건이었고, 큰 장애물 seed9~10에서는
  full 8/10, sector/adaptive 각각 6/10으로 격차가 줄었다.

### 해석

full은 공간 정보량은 많지만 한 메시지가 커서 ROG가 처리하는 시간 프레임 수가 줄어든다.
QoS가 최신 프레임 위주이므로 밀린 프레임은 누적되지 않고 교체될 수 있다. 반면 sector/adaptive는
후방·측면을 버리지만 충돌 위험 장애물이 있던 진행방향 ±60°는 유지하면서 맵을 더 자주
갱신했다. 미관측을 free로 계획하고 0.1 s 앞의 기존 궤적에 새 궤적을 연결하는 현 설정에서는
공간 커버리지보다 시간 신선도가 더 유리하게 작용할 수 있다.

단, 실행은 모드별로 독립이고 플래너 경로와 타이밍이 매번 달라진다. 따라서 15 대 7을
“sector가 본질적으로 full보다 안전하다”는 일반 법칙으로 주장하지 않고, 현 시스템에서
관찰된 **처리량–시간 신선도 trade-off**로 기록한다.

---

## 2026-08-04 — seed11 모드당 10회 독립 재검증

### 시도 이유와 방법

seed11은 SUPER가 공개한 `random_map_2_26609.pcd` dense-forest 지도이고 원 논문은 충돌 없는
안전 비행을 보고했다. 기존 모드당 5회 결과의 변동성을 줄이기 위해, 현재 프로젝트 스택을
바꾸지 않고 seed11×full/sector/adaptive×10회, 총 30회를 독립 CSV로 실행했다.

### 결과

전체 30/30이 완주했다. 접촉이 한 번이라도 표시된 런은 full 6/10, sector 1/10,
adaptive 2/10이었다. 원시 transition 합계는 12/1/4이지만 표면점 전이 중복 가능성 때문에
헤드라인에 쓰지 않았다. 평균 점/frame은 67,749/21,435/21,757, ROG 총 mapping은
14.762/6.389/6.269 ms, 실효 맵 갱신률은 2.26/3.51/3.92 Hz였다. 미션시간은
73.32/73.41/73.83 s로 동일한 수준이었다.

### 원 논문 0충돌과의 해석 경계

확인 결과 seed11은 **원 논문과 같은 PCD와 MARSIM**이지만, 현재 실행은 **원 논문 스택을
그대로 재현한 것이 아니다.** 현 `static_reference.yaml`은 `/cloud_registered`를 Python 필터가 받은 뒤
`/cloud_sector`로 다시 발행하고 full도 이 복사·직렬화·QoS 경로를 거친다. upstream 추적
`static_dense.yaml`은 raw `/cloud_registered`를 직접 구독하고 `max_vel=8.0`을 사용한다.
또한 현재 SUPER 소스에도 ROS2 이식·FOV 기능 변경이 있다.

따라서 이번 full 6/10은 “SUPER 원 논문의 0충돌 주장이 틀렸다”는 재현 실패 근거가 아니라,
“원본 지도에 본 프로젝트의 외부 필터 파이프라인을 적용한 결과”로만 해석한다. 원 논문과 직접
비교하려면 다음 제어실험으로 upstream raw-direct baseline을 동일 충돌 모니터로 10회 실행해야 한다.
